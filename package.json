{
  "private": true,
  "engines": {
    "node": ">=4"
  },
  "eslintConfig": {
    "parserOptions": {
      "ecmaVersion": 6,
      "sourceType": "module"
    },
    "env": {
      "es6": true,
      "node": true,
      "browser": true
    },
    "rules": {}
  },

  "scripts": {
    "start": "npm install gulp && npm install browser-sync && npm install gulp-sass && gulp serve"
  }
}
